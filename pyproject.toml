[project]
name = "fastapi-crud"
version = "0.1.0"
description = "FastAPI CRUD example with SQLModel, PostgreSQL, JWT auth"
requires-python = ">=3.14"

[tool.uv]
# uv manages dependency resolution for this project.
